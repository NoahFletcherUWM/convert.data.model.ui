import React from "react";
import { Typography, Paper } from "@mui/material";

const Settings = () => {
  return (
    <Paper>
      <Typography style={{ marginTop: 50 }}>Settings</Typography>
    </Paper>
  );
};

export default Settings;
